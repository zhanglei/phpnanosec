PHP Nanoseconds function
=========================

```bash
/usr/bin/phpize
./configure --with-nanosec --enable-nanosec --with-php-config=/usr/bin/php-config
make && make install
```


call nanosec function in PHP 
```php
<?php
echo nanosec();
```
